# Netpeak Request Classifier

A Python service that classifies incoming requests from internal teams using LLM (Anthropic Claude API). Reads requests from CSV, classifies them into structured format, and generates aggregated reports.

**Status:** Work in progress.

## Setup

Instructions will be added soon.